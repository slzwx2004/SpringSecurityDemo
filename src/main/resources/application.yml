spring:
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ***
            client-secret: ***
            redirect-uri: http://localhost:8080/login/oauth2/code/github
server:
  port: 8080
  servlet:
    context-path: /
